You need Node.js to run `node filler.js`. Running it will make requests to *osu!*'s API using the key in the .env file in this folder, if there is one.

Afterwards, it will generate an "index.html" file in the parent folder.